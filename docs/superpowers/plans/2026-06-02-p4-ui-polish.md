# P4: UI 适度打磨 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复已知 UI bug，提升暗色主题一致性，添加键盘快捷键。

**Architecture:** 修复现有组件 bug，统一 CSS 变量，添加全局键盘快捷键处理。

**Tech Stack:** Vue 3, Element Plus, CSS Variables

---

### Task 1: 修复 EntryDetailView 分组显示

**Files:**
- Modify: `src/views/EntryDetailView.vue`

- [ ] **Step 1: 使用 groups store 显示分组名称**

在 template 中将：

```html
<div class="field">
  <label>分组</label>
  <div class="field-value">{{ entry.group_id }}</div>
</div>
```

改为：

```html
<div class="field">
  <label>分组</label>
  <div class="field-value">{{ getGroupName(entry.group_id) }}</div>
</div>
```

在 script 中添加（如果 P2 已完成则已有）：

```javascript
import { useGroupsStore } from '@/stores/groups'
const groupsStore = useGroupsStore()

function getGroupName(groupId) {
  const group = groupsStore.groups.find(g => g.id === groupId)
  return group ? group.name : groupId
}

onMounted(async () => {
  await groupsStore.loadGroups()
  // ... existing code
})
```

- [ ] **Step 2: Commit**

```bash
git add src/views/EntryDetailView.vue
git commit -m "fix: display group name instead of group_id in entry detail"
```

---

### Task 2: 修复 DashboardView favorites 路由监听

**Files:**
- Modify: `src/views/DashboardView.vue`

- [ ] **Step 1: 修复 watch 监听 favorites 参数**

将：

```javascript
watch(() => route.query, () => {
  if (route.query.group) {
    loadEntries({ group_id: route.query.group })
  }
})
```

改为：

```javascript
watch(() => route.query, (query) => {
  if (query.group) {
    entriesStore.loadEntries({ group_id: query.group })
  } else if (query.favorites) {
    entriesStore.loadEntries({ favorites: true })
  } else {
    entriesStore.loadEntries()
  }
}, { immediate: true })
```

- [ ] **Step 2: Commit**

```bash
git add src/views/DashboardView.vue
git commit -m "fix: properly handle favorites query parameter in dashboard watch"
```

---

### Task 3: 清理 MainLayout 空函数

**Files:**
- Modify: `src/layouts/MainLayout.vue`

- [ ] **Step 1: 移除空的 startDrag 函数**

删除 `startDrag` 函数定义和 `@mousedown` 绑定（如果存在）。

- [ ] **Step 2: Commit**

```bash
git add src/layouts/MainLayout.vue
git commit -m "fix: remove empty startDrag function from MainLayout"
```

---

### Task 4: 统一暗色主题 CSS 变量

**Files:**
- Modify: `src/styles/global.css`

- [ ] **Step 1: 审计并补充 Element Plus 暗色覆盖**

在 `src/styles/global.css` 中确保以下覆盖存在：

```css
/* Element Plus 暗色主题覆盖 */
:root {
  --el-bg-color: var(--bg-secondary);
  --el-bg-color-overlay: var(--bg-tertiary);
  --el-bg-color-page: var(--bg-primary);
  --el-text-color-primary: var(--text-primary);
  --el-text-color-regular: var(--text-secondary);
  --el-text-color-secondary: var(--text-secondary);
  --el-border-color: var(--border);
  --el-border-color-light: var(--border);
  --el-border-color-lighter: var(--border);
  --el-fill-color-blank: var(--bg-tertiary);
  --el-fill-color-light: var(--bg-tertiary);
  --el-color-primary: var(--accent);
  --el-color-danger: var(--danger);
  --el-color-warning: #e6a23c;
  --el-color-success: #67c23a;
  --el-mask-color: rgba(0, 0, 0, 0.6);
}

/* 对话框暗色 */
.el-dialog {
  background: var(--bg-secondary) !important;
  border: 1px solid var(--border) !important;
}
.el-dialog__title { color: var(--text-primary) !important; }
.el-dialog__headerbtn .el-dialog__close { color: var(--text-secondary) !important; }

/* 输入框暗色 */
.el-input__wrapper {
  background: var(--bg-tertiary) !important;
  box-shadow: 0 0 0 1px var(--border) inset !important;
}
.el-input__inner { color: var(--text-primary) !important; }

/* 下拉选择器暗色 */
.el-select-dropdown {
  background: var(--bg-tertiary) !important;
  border-color: var(--border) !important;
}
.el-select-dropdown__item { color: var(--text-primary) !important; }
.el-select-dropdown__item.hover, .el-select-dropdown__item:hover {
  background: var(--bg-hover) !important;
}

/* 右键菜单暗色覆盖已在 scoped 中处理 */

/* 按钮暗色 */
.el-button--default {
  background: var(--bg-tertiary) !important;
  border-color: var(--border) !important;
  color: var(--text-primary) !important;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/styles/global.css
git commit -m "style: unify dark theme CSS variables for Element Plus components"
```

---

### Task 5: 添加全局键盘快捷键

**Files:**
- Modify: `src/layouts/MainLayout.vue`

- [ ] **Step 1: 添加键盘快捷键处理**

在 MainLayout script 中添加：

```javascript
import { onMounted, onUnmounted } from 'vue'

function handleKeydown(e) {
  // Ctrl+N: 新建条目
  if (e.ctrlKey && e.key === 'n') {
    e.preventDefault()
    router.push('/app')
    // 触发 DashboardView 的新建对话框
    // 通过事件总线或 route query 实现
  }

  // Ctrl+F: 聚焦搜索框
  if (e.ctrlKey && e.key === 'f') {
    e.preventDefault()
    const searchInput = document.querySelector('.search-input')
    if (searchInput) searchInput.focus()
  }

  // Ctrl+L: 锁定应用
  if (e.ctrlKey && e.key === 'l') {
    e.preventDefault()
    lockApp()
  }

  // Escape: 关闭弹窗
  if (e.key === 'Escape') {
    // 关闭所有 Element Plus 弹窗
    document.querySelectorAll('.el-overlay').forEach(el => el.click())
  }
}

onMounted(() => {
  document.addEventListener('keydown', handleKeydown)
})

onUnmounted(() => {
  document.removeEventListener('keydown', handleKeydown)
})
```

- [ ] **Step 2: Commit**

```bash
git add src/layouts/MainLayout.vue
git commit -m "feat: add global keyboard shortcuts

Ctrl+N: New entry, Ctrl+F: Focus search, Ctrl+L: Lock, Escape: Close dialogs"
```

---

### Task 6: P4 最终验证

- [ ] **Step 1: 手动验证清单**

1. EntryDetailView 显示分组名称而非 ID
2. 点击侧边栏"收藏"链接，Dashboard 正确过滤
3. 暗色主题下所有 Element Plus 组件颜色一致
4. Ctrl+F 聚焦搜索框，Ctrl+L 锁定应用
5. Escape 关闭弹窗

- [ ] **Step 2: 最终 Commit**

```bash
git add -A
git commit -m "chore: P4 UI polish complete

- Fix group name display in entry detail
- Fix favorites route query handling
- Unify dark theme CSS variables
- Add global keyboard shortcuts (Ctrl+N/F/L, Escape)"
```
